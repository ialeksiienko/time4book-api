services:
  api:
    container_name: time4book-api
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env:ro
    ports:
      - "50052:50052"
    restart: unless-stopped
